{
  "scripts": {
    "test": "echo $DOCKER_HOST",
    "build": "docker run --rm -v $PWD:/app hut6/webpack:1.4",
    "dev-server": "docker run --rm -v $PWD:/app -p 8080:8080 -e ASSET_PATH=\"http://${WEBPACK_HOST:-$(docker-machine ip)}:9090/\" hut6/webpack:1.4 webpack-dev-server --mode development --env.devServer",
    "yarn": "docker run -v $PWD:$PWD -w $PWD hut6/yarn"
  }
}
