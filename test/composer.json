{
  "scripts": {
    "build": "docker run --rm -v $PWD:/app hut6/webpack:1.2",
    "dev-server": "docker run --rm -v $PWD:/app -p 8080:8080 -e ASSET_PATH=\"http://$(docker-machine ip):8080/\" hut6/webpack:1.2 webpack-dev-server --mode development",
    "yarn": "docker run -v $PWD:$PWD -w $PWD hut6/yarn"
  }
}
